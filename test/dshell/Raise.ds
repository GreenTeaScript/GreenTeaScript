require dshell;

let DCaseRevision = 2;
let Location="WebServer";
let RecServer="https://192.168.0.100:8080/rec/";
let AssumedFault = UnknownFault;

DFault CallAdmin() {
    raise AssumedFault;
    return null;
}

@Export int main() {
    DFault fault = CallAdmin();
    assert(fault != null);
    return 0;
}
